import React from 'react'
//Context is nothing but global provider inside that whatever will be done that will goes for all 
// it will take all state excess
const UserContext=React.createContext()

export default UserContext;